name: CI
on: [pull_request]
jobs:
  lint:
    uses: ./.github/workflows/foo.yml
  test:
    uses: ./.github/workflows/bar.yml
    needs: lint
  build:
    uses: ./.github/workflows/bar.yml
    needs: test
